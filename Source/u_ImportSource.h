//---------------------------------------------------------------------------
// Табличный источник данных для импорта. Первая реализация — чтение .xlsx
// (открытие как zip+xml средствами System.Zip). Реализация — в задаче «Импорт».
//---------------------------------------------------------------------------
#ifndef u_ImportSourceH
#define u_ImportSourceH
//---------------------------------------------------------------------------
#include <System.hpp>
#include <vector>
//---------------------------------------------------------------------------
class TXlsxSource
{
public:
	TXlsxSource();
	~TXlsxSource();

	// Открыть файл AFileName и прочитать первый лист в память.
	void Open(const String &AFileName);
	void Close();

	// Число прочитанных строк (включая строку заголовка).
	int RowCount() const;

	// Ячейки строки AIndex (0-based). Пустой вектор при выходе за диапазон.
	std::vector<String> Row(int AIndex) const;

private:
	std::vector<std::vector<String> > FRows;
};
//---------------------------------------------------------------------------
#endif
