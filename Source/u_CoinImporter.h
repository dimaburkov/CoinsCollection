//---------------------------------------------------------------------------
// Преобразование строк табличного файла (экспорт uCoin.net) в TCoinRecord:
// сопоставление колонок, разбор чисел (десятичная запятая) и дат
// (dd/mm/yyyy, "00/01/1900" -> нет). Реализация — в задаче «Импорт».
//---------------------------------------------------------------------------
#ifndef u_CoinImporterH
#define u_CoinImporterH
//---------------------------------------------------------------------------
#include <System.hpp>
#include <vector>
#include "u_CoinTypes.h"
//---------------------------------------------------------------------------
class TXlsxSource;
//---------------------------------------------------------------------------
class TCoinImporter
{
public:
	TCoinImporter();
	~TCoinImporter();

	// Разобрать все строки источника. Ошибки складываются в Errors().
	std::vector<TCoinRecord> Parse(TXlsxSource &ASource);

	// Сообщения об ошибках последнего вызова Parse().
	const std::vector<String> &Errors() const;

private:
	std::vector<String> FErrors;
};
//---------------------------------------------------------------------------
#endif
